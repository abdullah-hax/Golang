package defer1

/*

var a
defer fmt.Println(a)  // in there, fmt is package & Println is function

defer function isn't executed immidiately, it is stored first. Go runtime runs the function (ectually everything is executed by cpu) just before executing return


*/
